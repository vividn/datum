describe("durationView", () => {
  it("emits nothing if just occurTime with no dur or state", async () => {

  });
  it("emits ")
  test.todo("add in a bunch of various datum documents and the snapshot test the resulting view");
})

// need a duration view
// hmmm.
// for the key it should emit [field, year, month