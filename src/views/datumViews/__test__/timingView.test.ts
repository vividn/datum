describe("timingView", () => {
  it.todo(
    "emits occur, modify, and createTime for field and for null (all fields)"
  );
  it.todo("emits occur, modify and createTime if there is no field");
  it.todo(
    "does not emit occurTime, if occurTime is a just a date, but still emits modify and createTime"
  );
  it.todo("does not emit occurTime if there is no occurTime");
});
