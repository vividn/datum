<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg height="100" width="800" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns="http://www.w3.org/2000/svg">
    <defs>
        <style>
            svg { overflow: visible; }
        </style>
    </defs>
    <rect fill="black" height="100%" width="100%"/>
    <svg height="96" width="796" y="2" x="2" class="plot">
        <svg x="0" height="81" width="796" class="nowArea">
            <svg class="timechunk">
                <defs/>
                <svg height="9.719999999999999" width="796" y="4.219771737568081" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="text">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="17.59549516612664" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="project">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="35.171050295569" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="stretch">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="42.071827930267894" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="pushup">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="47.40763666584156" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="run">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="59.04751210553571" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="sleep">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="68.27553918711841" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="caffeine">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
                <svg height="9.719999999999999" width="796" y="68.86375723603182" x="0" preserveAspectRatio="none" viewBox="0 0 796 9.719999999999999" class="environment">
                    <defs>
                        <style>
                            .popover {
      position: absolute;
      background: rgba(0, 0, 0, 0.8);
      color: white;
      padding: 8px;
      border-radius: 4px;
      font-family: sans-serif;
      font-size: 12px;
      pointer-events: none;
      white-space: pre-line;
      z-index: 1000;
      max-width: 250px;
      display: none;
    }
                        </style>
                    </defs>
                    <rect data-hover-text="Field: environment
State: home
Time: 15:18 - 15:18
Duration: 0m" data-end-time="2025-07-28T15:18:25.436Z" data-time="2025-07-28T15:18:25.435Z" data-state="home" data-field="environment" height="9.719999999999999" width="796" y="0" x="0" fill="#106a6c" class="environment home block"/>
                    <script type="text/javascript">
                        (function() {
      // Create popover element
      const popover = document.createElement('div');
      popover.className = 'popover';
      document.body.appendChild(popover);

      // Add event listeners to all elements with hover text
      const hoverElements = document.querySelectorAll('[data-hover-text]');
      hoverElements.forEach(el =&gt; {
        el.addEventListener('mouseover', (e) =&gt; {
          const text = e.target.getAttribute('data-hover-text');
          popover.textContent = text;
          popover.style.display = 'block';
        });

        el.addEventListener('mousemove', (e) =&gt; {
          // Position the popover near the cursor
          popover.style.left = (e.pageX + 10) + 'px';
          popover.style.top = (e.pageY + 10) + 'px';
        });

        el.addEventListener('mouseout', () =&gt; {
          popover.style.display = 'none';
        });
      });
    })();
                    </script>
                </svg>
            </svg>
        </svg>
        <text font-weight="bold" font-size="17px" fill="white" dominant-baseline="middle" text-anchor="middle" y="90.75" x="398" class="current-time-text">
            15:18
        </text>
    </svg>
</svg>