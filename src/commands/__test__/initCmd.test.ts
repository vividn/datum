describe("initCmd", () => {
  it("fails without the --global flag for now");
  it("writes datumrc into the xdg_config directory");
  it("fails if datumrc exists and --overwrite is not specified");
  it("overwrites datumrc if it exists and --overwrite is specified");
});
