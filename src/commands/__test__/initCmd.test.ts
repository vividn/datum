describe("initCmd", () => {
  it("writes datumrc into the xdg_config directory", async () => {

  });
  it("fails if datumrc exists and --overwrite is not specified");
  it("overwrites datumrc if it exists and --overwrite is specified");
  it("creates the project directory, and populates it with some files");
});
