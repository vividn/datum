describe("switchCmd", () => {
  it.todo("creates an occur document with a custom state");
  it.todo("also records what it thinks the last state was");
  it.todo("can also take in a manually specified last state");
  it.todo("does not include a lastState if there is no occurTime")
  it.todo("still includes lastState if manually specified even when there is no occurTime")
});