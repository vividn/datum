it.todo("emits a datum v1 style table with a header");

it.todo("emits all fields, with field as the first column if no field is given");
it.todo("emits specified fields with field as first column if multiple fields given");

it.todo("has the appropriate columns and data for activity");
it.todo("has the appropriate columns and data for environment");
it.todo("has the appropriate columns and data for call");
it.todo("has the appropriate columns and data for consume");
it.todo("has the appropriate columns and data for hygiene");
it.todo("has the appropriate columns and data for others???");

it.todo("writes field v1 to specified outputFile");
it.todo("writes to {field}.tsv if outputDir is specified");
it.todo("writes a datum-v1 like file in an outputFile");
it.todo("writes one datum-v1 like file for each field if outputDir is specified");
